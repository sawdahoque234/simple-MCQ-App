import quizData from "./quizData";
const mcqSection = document.querySelector(".mcqSection");

quizData.map((quiz) =>
  // const {title,options,answer}=quiz
  // retun{
  //     `
  //     <h1>{quiz.title}</h1>
  //     `
  // }
  console.log(quiz)
);
